{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Shop\Category list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Caption</th>
                <th>Published</th>
                <th>Deleted</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('shop_category_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.caption }}</td>
                <td>{{ entity.published }}</td>
                <td>{{ entity.deleted }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('shop_category_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('shop_category_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('shop_category_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
