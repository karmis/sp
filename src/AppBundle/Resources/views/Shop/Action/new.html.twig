{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Shop\Action creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('shop_action') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
